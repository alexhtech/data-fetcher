import Fetcher, { defaults } from './Fetcher';
const { fetcher, config, stringifyQuery, parseQuery } = new Fetcher();
export { defaults };
export { fetcher, config, stringifyQuery, parseQuery, fetcher as default };
